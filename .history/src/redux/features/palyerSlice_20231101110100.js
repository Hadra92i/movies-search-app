import { createSlice } from '@reduxjs/toolkit'

const initialState = {
    
}

export const PlayerSlice = createSlice({
    name : "player",
    initialState,
    reducers : {},
    extraReducers : {

    }
});

